<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add New Brand - GrocerGo</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-muted text-foreground font-sans">
    <div class="flex min-h-screen">
        <!-- Sidebar with active highlight -->
        <%- include('./partials/sidebar', { currentPath: '/admin/brands' }) %>

        <main class="flex-1 p-6 md:p-8">
            <div class="max-w-3xl mx-auto space-y-8">

                <!-- Page Header -->
                <div>
                    <h1 class="text-3xl font-bold mb-1">Add a New Local Brand</h1>
                    <p class="text-sm text-muted-foreground">
                        Onboard a brand so their products can appear across GrocerGo categories.
                    </p>
                </div>

                <!-- Card -->
                <section class="bg-card border border-border shadow-md rounded-xl p-6 md:p-8 admin-form-card">
                    <form action="/admin/brands/new" method="POST" class="admin-form space-y-5">
                        <!-- Brand Name -->
                        <div class="admin-form-group">
                            <label class="admin-form-label" for="name">Brand Name</label>
                            <input 
                                type="text"
                                id="name"
                                name="name"
                                required
                                placeholder="e.g., FreshHarvest Organics"
                            >
                        </div>

                        <!-- Logo URL -->
                        <div class="admin-form-group">
                            <label class="admin-form-label" for="logo">Logo URL</label>
                            <input 
                                type="url"
                                id="logo"
                                name="logo"
                                required
                                placeholder="https://example.com/logo.png"
                            >
                        </div>

                        <!-- Category -->
                        <div class="admin-form-group">
                            <label class="admin-form-label" for="category">
                                Primary Category 
                                <span class="admin-form-label-hint">(e.g., Vegetables, Snacks, Dairy)</span>
                            </label>
                            <input 
                                type="text"
                                id="category"
                                name="category"
                                required
                                placeholder="e.g., Bakery"
                            >
                        </div>

                        <!-- Buttons -->
                        <div class="admin-form-actions">
                            <a href="/admin/brands" class="admin-btn-secondary">
                                Cancel
                            </a>
                            <button type="submit" class="admin-btn-primary">
                                Save Brand
                            </button>
                        </div>
                    </form>
                </section>

            </div>
        </main>
    </div>
</body>
</html>
