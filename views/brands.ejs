<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Local Brands - GrocerGo</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-background text-foreground font-sans">

<%- include('partials/header') %>

<section class="bg-gradient-to-r from-primary to-green-700 text-white py-12">
    <div class="container mx-auto px-4">
        <div class="text-center">
            <h1 class="text-4xl font-bold mb-4">Our Verified Local Brands</h1>
            <p class="text-xl text-white/90 max-w-2xl mx-auto">
                Meet the trusted local suppliers who bring you premium quality products at prices you'll love.
            </p>
        </div>
    </div>
</section>

<section class="py-12">
    <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            <% brands.forEach(brand => { %>
            <div class="bg-card border border-border rounded-xl p-6 hover:shadow-lg transition-shadow">
                <div class="flex items-start space-x-4 mb-4">
                    <img src="<%= brand.logo %>" alt="<%= brand.name %> logo" class="w-16 h-16 object-cover rounded-lg" />
                    <div class="flex-1">
                        <h3 class="text-xl font-bold"><%= brand.name %></h3>
                        <p class="text-muted-foreground">Specializes in <strong><%= brand.category %></strong></p>
                    </div>
                </div>
            </div>
            <% }) %>
        </div>
    </div>
</section>


<%- include('partials/footer') %>
<%- include('partials/cart-sidebar') %>

<script src="/js/app.js"></script>
</body>
</html>
