<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Local Brands - GrocerGo</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-background text-foreground font-sans">

<%- include('partials/header') %>

<% const brandCount = brands ? brands.length : 0; %>

<!-- HERO -->
<section class="brands-hero">
    <div class="container mx-auto px-4">
        <div class="text-center max-w-2xl mx-auto">
            <h1 class="brands-hero-title">Our Verified Local Brands</h1>
            <p class="brands-hero-subtitle">
                Meet the trusted local suppliers who bring you premium quality products at prices you'll love.
            </p>

            <div class="brands-hero-chip-row">
                <span class="brands-hero-chip">
                    <i class="fas fa-shield-check"></i>
                    <span>Verified quality partners</span>
                </span>
                <span class="brands-hero-chip">
                    <i class="fas fa-map-marker-alt"></i>
                    <span>Supporting neighbourhood businesses</span>
                </span>
                <span class="brands-hero-count">
                    <strong><%= brandCount %></strong> brand<%= brandCount === 1 ? '' : 's' %> onboarded
                </span>
            </div>
        </div>
    </div>
</section>

<!-- BRANDS GRID -->
<section class="py-12 brands-section">
    <div class="container mx-auto px-4">
        <% if (!brands || brands.length === 0) { %>
            <div class="brands-empty-state">
                <div class="brands-empty-icon">
                    <i class="fas fa-store-slash"></i>
                </div>
                <h2>No brands onboarded yet</h2>
                <p class="brands-empty-text">
                    We’re actively partnering with local stores and producers. 
                    Check back soon to see featured brands in your area.
                </p>
                <a href="/products" class="brands-empty-cta">
                    Browse available products
                </a>
            </div>
        <% } else { %>
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
                <% brands.forEach(brand => { %>
                    <article class="brand-card">
                        <div class="brand-card-header">
                            <div class="brand-logo-shell">
                                <img 
                                    src="<%= brand.logo %>" 
                                    alt="<%= brand.name %> logo" 
                                    class="brand-logo"
                                />
                            </div>
                            <div class="flex-1 min-w-0">
                                <h3 class="brand-name"><%= brand.name %></h3>
                                <p class="brand-category">
                                    Specializes in <strong><%= brand.category %></strong>
                                </p>
                                <div class="brand-tags">
                                    <span class="brand-tag">
                                        <i class="fas fa-badge-check"></i>
                                        Verified local brand
                                    </span>
                                </div>
                            </div>
                        </div>

                        <div class="brand-footer">
                            <div class="brand-footer-left">
                                <span class="brand-footer-icon">
                                    <i class="fas fa-basket-shopping"></i>
                                </span>
                                <p class="brand-footer-text">
                                    Explore products from <strong><%= brand.name %></strong> in relevant categories.
                                </p>
                            </div>
                        </div>
                    </article>
                <% }) %>
            </div>
        <% } %>
    </div>
</section>

<%- include('partials/footer') %>
<%- include('partials/cart-sidebar') %>

<script src="/js/app.js"></script>
</body>
</html>
