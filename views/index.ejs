<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GrocerGo - Fresh Local Brands, Fast Delivery</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="bg-background text-foreground font-sans">

<%- include('partials/header') %>

<!-- Delivery Banner -->
<div class="bg-gradient-to-r from-accent to-orange-500 text-white py-2">
    <div class="container mx-auto px-4">
        <div class="flex items-center justify-center space-x-4 text-sm">
            <div class="delivery-pulse">
                <i class="fas fa-bolt"></i>
            </div>
            <span class="font-medium">Lightning Fast Delivery in 10-30 minutes | </span>
            <span>🚀 Free delivery on orders above ₹199</span>
        </div>
    </div>
</div>

<!-- Category Navigation -->
<nav id="categories" class="bg-card border-b border-border py-4 sticky top-16 z-40">
    <div class="container mx-auto px-4">
        <div class="flex overflow-x-auto space-x-6 text-sm font-medium category-nav">
            <% categories.forEach(category => { %>
            <a href="/category/<%= category.slug %>" 
               class="flex flex-col items-center space-y-1 min-w-0 whitespace-nowrap hover:text-primary transition-colors">
                <div class="w-12 h-12 bg-green-100 rounded-full flex items-center justify-center">
                    <i class="<%= category.icon %> text-green-600 text-xl"></i>
                </div>
                <span><%= category.name %></span>
            </a>
            <% }) %>
        </div>
    </div>
</nav>

<!-- Hero Banner -->
<section class="bg-gradient-to-br from-primary to-green-700 text-white py-8">
    <div class="container mx-auto px-4">
        <div class="grid md:grid-cols-2 gap-8 items-center">
            <div>
                <h1 class="text-4xl md:text-5xl font-bold mb-4">
                    Local Brands,<br>
                    <span class="text-accent">Global Quality</span>
                </h1>
                <p class="text-xl mb-6 opacity-90">
                    Discover premium local products at unbeatable prices. Verified quality, guaranteed freshness.
                </p>
                <div class="flex flex-col sm:flex-row gap-4">
                    <a href="/products" class="bg-accent hover:bg-orange-500 text-accent-foreground px-8 py-3 rounded-lg font-semibold transition-colors text-center inline-block">
                        Start Shopping
                    </a>
                    <a href="/brands" class="border-2 border-white text-white hover:bg-white hover:text-primary px-8 py-3 rounded-lg font-semibold transition-colors text-center inline-block">
                        View Local Brands
                    </a>
                </div>
            </div>
            <div class="hidden md:block">
                <img src="https://images.unsplash.com/photo-1542838132-92c53300491e?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&h=600" 
                     alt="Fresh Indian groceries and vegetables" 
                     class="rounded-xl shadow-2xl" />
            </div>
        </div>
    </div>
</section>

<!-- ... (rest of the content remains the same) ... -->

<%- include('partials/footer') %>
<%- include('partials/cart-sidebar') %>

<script src="/js/app.js"></script>
</body>
</html>

